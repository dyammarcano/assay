---
source: crates/core/src/scaffold.rs
expression: out.rust
---
// Generated by assay scaffold. Review before use.
pub fn register(builder: tauri::Builder<tauri::Wry>) -> tauri::Builder<tauri::Wry> {
    builder
    // Global shortcuts (electron.global_shortcut) — official plugin
    .plugin(tauri_plugin_global_shortcut::Builder::new().build())
    // Main/renderer IPC (electron.ipc) — no proven recipe; implement manually
    // stub:
    // fn electron_ipc() { todo!("implement electron.ipc") }
    // Toast notifications (uwp.toast) — proven crate: tauri-winrt-notification
    // TODO wire tauri-winrt-notification; see crate docs
    // Background tasks (uwp.background_tasks) — no proven recipe; implement manually
    // stub:
    // fn uwp_background_tasks() { todo!("implement uwp.background_tasks") }
}
